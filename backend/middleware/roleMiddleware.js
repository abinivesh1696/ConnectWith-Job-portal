// Role check middleware placeholder
// Usage: ensureRole('recruiter') to protect recruiter-only routes

// Implementation will be added in Step 6/12/13
export const ensureRole = (role) => (req, res, next) => {
  // placeholder
  next();
};
